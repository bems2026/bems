---
title: Evidence ledger
purpose: Every observation the manual may cite, with the command or file that produced it
audience: [integrator, administrator]
status: Draft
last_verified: 2026-09-23
applies_to: repo 2f4c570 (workstation) · edge checkout fcb1ff6
evidence: []
---

# Evidence ledger

Every technical statement in the manual carries one of these IDs or `[UNVERIFIED]`. Rows are
**appended, never renumbered**. A later check that corrects a row adds a new row that cites the old one. The old row
keeps its text and gains only a status pointer: "Superseded by E-NNN".

**Conventions.**
- **Source.** `Repo` is the working tree at `applies_to`. `Edge` is a read-only command run on the edge server over the mesh
  network, from the checkout at `<repo-dir>`. `Flow` is the live Node-RED flow, read from disk on the
  edge and redacted there with `node-red-bridge/redactFlow.mjs`. `DB` is a read-only REST `GET` using the
  edge's own `server/.env`, following the recipe in `pi-session-brief.md`. `Web` is a vendor's current documentation.
- **Raw output.** Each command's output was saved under `docs/audit/raw/`, which is gitignored and never committed.
  The file named in brackets is the capture that holds it. Host, user, addresses and keys are written as placeholders here.
- **Status.** `Confirmed` means it was observed. `Hypothesis` means it was reasoned from what was observed but not seen directly.
  `Unverified` means it could not be checked this session. `Stated` means the operator said so and nothing was inspected;
  it is cited as the operator's statement, never as an observation. `Hypothesis` is always stated as such, never as fact.
- **Times.** All times are in the facility's local zone (UTC+08:00) unless marked UTC.

## Access and baseline

| ID | Claim | Source | Command or file | Date | Status |
|---|---|---|---|---|---|
| E-001 | Before this work, the workstation checkout matched `origin/master` at `15aa65e`. | Repo | `git fetch; git rev-parse --short HEAD origin/master` | 2026-09-23 | Confirmed |
| E-002 | The edge server answers on the mesh network. The path is relayed, not direct (3/3 pongs, 165–1038 ms). | Edge | `tailscale ping -c 3 $EDGE_HOST` | 2026-09-23 | Confirmed |
| E-003 | The edge checkout is at `fcb1ff6`, one commit behind `origin/master`. That commit, `15aa65e`, changes only `ROADMAP.md`. The tree is clean and there are no local-only commits. | Edge | `git rev-parse HEAD; git status -sb; git log --oneline -5` [edge-capture §repo-head] | 2026-09-23 | Confirmed |
| E-004 | The kiosk serves bundle `assets/index-DGcKHoNp.js`, built 2026-09-23 20:57. | Edge | `curl -s http://127.0.0.1:5183/ \| grep -o 'assets/index-[^"]*'` [edge-capture §kiosk-bundle] | 2026-09-23 | Confirmed |
| E-006 | Re-checked at 05:56 on 2026-09-24, before office hours. The broker is still on `0.0.0.0:1883`, both config files keep their 2026-09-17 mtimes, and the SoC reads **77.9 °C** with `get_throttled=0xe0000`. The scheduler still reports `dispatch=OPEN`, all seven iBEMS-related units are active, and the edge checkout is still `fcb1ff6`. | Edge | `ss -tln`; `ls -la --time-style=long-iso /etc/mosquitto/…`; `vcgencmd measure_temp; vcgencmd get_throttled`; `systemctl is-active …; systemctl --user is-active ibems-kiosk` | 2026-09-24 | Confirmed |
| E-005 | A long-running Claude Code session is present on the edge: two `claude` processes, one of them listening on loopback. | Edge | `pgrep -a claude; ss -tulnp` [edge-capture §claude-processes] | 2026-09-23 | Confirmed |

## Edge platform (L3)

| ID | Claim | Source | Command or file | Date | Status |
|---|---|---|---|---|---|
| E-010 | OS: Debian GNU/Linux 13.6 (trixie). Kernel 6.18.39+rpt-rpi-v8, aarch64. | Edge | `cat /etc/os-release; uname -a` [edge-capture §os] | 2026-09-23 | Confirmed |
| E-011 | Hardware: Raspberry Pi 4 Model B Rev 1.4 with 7.6 GiB RAM. Swap is 2 GiB zram, unused. | Edge | `cat /proc/device-tree/model; free -h` [§hardware-model] | 2026-09-23 | Confirmed |
| E-012 | The root filesystem is ext4 (`noatime`) on a 128 GB SD card. The card reports name `SC128`, manufacturer ID `0x000003` and date `06/2018`. 22 G of 117 G is used. The card's endurance class cannot be read from its ID. | Edge | `df -h; lsblk; findmnt /; cat /sys/block/mmcblk0/device/{name,manfid,date}` [§storage, §sd-card-id] | 2026-09-23 | Confirmed (endurance: Unverified) |
| E-013 | The 2026-09-22 boot ran `orphan cleanup on readonly fs` before remounting read-write, which indicates the previous shutdown was not clean. | Edge | `journalctl -k -b \| grep -iE 'mmc\|ext4'` [§storage-health-kernel-journal] | 2026-09-23 | Hypothesis |
| E-014 | There is no real-time clock (`/dev/rtc*` is absent). Time comes from `systemd-timesyncd` via a public NTP pool, and the clock is synchronised. The zone is Asia/Manila (UTC+08:00). | Edge | `ls /dev/rtc*; timedatectl; timedatectl show-timesync` [§rtc, §time] | 2026-09-23 | Confirmed |
| E-015 | At capture the SoC read 80.3 °C and `get_throttled` returned `0xe0000`. Per Raspberry Pi's documentation that means the soft temperature limit, throttling and ARM frequency capping **have occurred** since boot. None is active now, and there has been no under-voltage. | Edge + Web | `vcgencmd measure_temp; vcgencmd get_throttled` [§temperature-throttling]; raspberrypi.com/documentation/computers/os.html | 2026-09-23 | Confirmed |
| E-016 | The kiosk's Chromium processes used about 58 % CPU in total (33 % + 25 %) in one `top` sample, against a load average of about 2.6 on 4 cores. Averaged over their lifetime since 2026-09-22 07:42, the kiosk's renderer runs at 42 % and its GPU process at 32.5 % of one core, both on the kiosk's own profile. | Edge | `top -bn1 -o %CPU \| head -18`; `ps -eo pid,pcpu,args --sort=-pcpu` (lifetime average) [edge-capture-3 §top; edge-capture-5] | 2026-09-23 | Confirmed |
| E-017 | Node.js is v22.23.2 (NodeSource apt package) and npm 10.9.8. | Edge | `node -v; npm -v; dpkg -l nodejs` [§node-npm, capture-2 §update-policy] | 2026-09-23 | Confirmed |
| E-018 | Node-RED is 4.1.8, installed with global npm. The palette includes `node-red-contrib-tuya-smart-device` 5.4.0 (tuyapi 7.7.1), `node-red-contrib-google-sheets` 1.1.2, `node-red-contrib-solarman-devices` 1.0.4, `@binsoul/node-red-contrib-deye-sun-g3` ~0.1.0, `node-red-dashboard` 3.6.6, `node-red-contrib-influxdb` 0.7.0 and cron/scheduler nodes. | Edge | `npm ls -g --depth=0; cat ~/.node-red/package.json; grep version …/package.json` [§node-red-version, §node-red-palette-versions] | 2026-09-23 | Confirmed |
| E-019 | Node-RED `settings.js` sets `uiHost: "127.0.0.1"`, `adminAuth` and `credentialSecret` (a literal value, not read from the environment), with `contextStorage` on `localfilesystem`. `httpNodeAuth` is not set. Values were not read. | Edge | `grep -nE '^[[:space:]]*(adminAuth\|credentialSecret\|…)[[:space:]]*:' settings.js \| cut -d: -f1,2` [§node-red-settings-keys-uncommented, §…credentialSecret-kind] | 2026-09-23 | Confirmed |
| E-020 | `nodered.service` runs as the service user with `Restart=on-failure` after 20 s and `NODE_OPTIONS=--max_old_space_size=512`. A drop-in rate-limits its journal to 100 lines per 30 s. | Edge | `systemctl cat nodered.service` [§unit-cat-nodered] | 2026-09-23 | Confirmed |
| E-021 | `ibems-dashboard` runs `serve -s dist -l 5183`. `ibems-ingest`, `ibems-proxy` and `ibems-scheduler` each run `node server/<name>.mjs` with `EnvironmentFile=server/.env`. All four are `Type=simple` with `Restart=on-failure` after 10 s, ordered after `nodered`, active and at `NRestarts=0`. | Edge | `systemctl cat` / `systemctl show -p ActiveState,NRestarts,…` per unit [§unit-cat-*, §unit-status-*] | 2026-09-23 | Confirmed |
| E-022 | Active timers: `ibems-lan-map` (2 min after boot, then every 10 min), `ibems-fleet-recover` (12 min, then every 5 min) and `ibems-wifi-prefer` (90 s, then every 5 min, runs as root). A fourth, `tailscale-watchdog.timer`, runs `/usr/local/bin/tailscale-watchdog.sh` every 2 min and **is not in the repository**. | Edge + Repo | `systemctl list-timers --all; systemctl cat <timer>`; `git grep tailscale-watchdog` (no hits) [§timers, §timer-cat, capture-2 §tailscale-watchdog-unit] | 2026-09-23 | Confirmed |
| E-023 | The kiosk is a **systemd user unit**, `~/.config/systemd/user/ibems-kiosk.service`, **byte-identical to `server/ibems-kiosk.service`**. It is active and enabled, and has run since 2026-09-22 07:42 with `Restart=always`. It starts through `lightdm` autologin (a `labwc` session) into `default.target`, and runs Chromium `--kiosk` on a dedicated profile. It is absent from system scope, which is why `systemctl cat` without `--user` finds nothing. | Edge | `systemctl --user show/cat ibems-kiosk.service`; `diff` against the repo copy; `grep autologin /etc/lightdm/…` [edge-capture-5; capture-2 §kiosk-lightdm-autologin] | 2026-09-23 | Confirmed |
| E-024 | systemd is the only process supervisor (`pm2` is not installed). | Edge | `which pm2` [§process-supervision] | 2026-09-23 | Confirmed |
| E-025 | Listening sockets: Node-RED on loopback:1880; the proxy on all interfaces:8080; the dashboard on all interfaces:5183; **Mosquitto on 0.0.0.0:1883**; SSH on 22; VNC on 5900 on all interfaces; rpcbind on 111; CUPS on loopback; Tailscale Serve on the mesh address:443; and the proxy's UDP 6666/6667/7000 listeners for LAN presence. | Edge | `ss -tuln; ss -tulnp` [§listening-ports*] | 2026-09-23 | Confirmed |
| E-026 | Tailscale Serve is **tailnet only**: `/` → `localhost:5183`, `/api` and `/ws` → `localhost:8080`. Funnel is not enabled. | Edge | `tailscale serve status; tailscale funnel status` [capture-2 §tailscale-serve-funnel] | 2026-09-23 | Confirmed |
| E-027 | Tailscale 1.102.2. Tailscale SSH is on (`RunSSH: true`), no key expiry is set on the edge node, and auto-update is on (check and apply). The tailnet has 7 nodes: the edge server and 6 client devices. It reported the health warning "can't reach the configured DNS servers". | Edge | `tailscale status; tailscale status --self --json` (fields only); `tailscale debug prefs \| grep …` [§tailscale, capture-2 §tailscale-*] | 2026-09-23 | Confirmed |
| E-028 | Mosquitto 2.0.21 is active and enabled. In `mosquitto.conf` (mtime 2026-09-17 15:57) both loopback `listener` lines are **commented out**. `conf.d/bems.conf` (mtime 2026-09-17 15:47) declares `listener 1883 0.0.0.0` with `allow_anonymous true`. Timestamped `.bak` files from 2026-08-26 sit beside both files. | Edge | `ls -la --time-style=long-iso /etc/mosquitto /etc/mosquitto/conf.d; grep -Hnv '^#' …; cat …` [capture-2 §mosquitto-*, capture-3 §mosquitto-conf-full] | 2026-09-23 | Confirmed |
| E-029 | Nothing in the journal says who changed the broker on 2026-09-17 or why. The journal was volatile before 2026-09-22 (see E-035). | Edge | `journalctl --since '2026-09-17 15:30' --until '2026-09-17 16:05' \| grep -iE 'sudo\|mosquitto'` (no output) [capture-3 §who-changed-mosquitto-0917] | 2026-09-23 | Confirmed (absence) |
| E-030 | The only MQTT nodes in the live flow are one `mqtt-broker` config node and one `mqtt in` node ("ESP32 AC Sniffer") on the Aircon tab, **which is disabled**. | Flow | node-type filter over `flows.json` [capture-3 §solarman-node-location] | 2026-09-23 | Confirmed |
| E-031 | Docker CE 29.7.2 and containerd are installed, enabled and running, with **zero containers**. The service user is in the `docker` group. | Edge | `docker ps -a; docker compose ls; id; dpkg -l \| grep docker` [capture-2 §docker-containers, §groups] | 2026-09-23 | Confirmed |
| E-032 | None of Home Assistant, PostgreSQL/TimescaleDB, InfluxDB server, Grafana or n8n is installed on the edge. A backup directory named `n8n-decommission-20260816-…` exists under `~/backups`. | Edge | `which docker hass n8n psql; systemctl list-units --all \| grep -iE …; dpkg -l \| grep -iE …; ls ~/backups` [§removed-components-*, capture-2 §backup-artifacts] | 2026-09-23 | Confirmed |
| E-033 | VNC (`wayvnc`) listens on all interfaces (`address=::`) with `enable_auth=true` and PAM, so the service user's password is the credential. | Edge | `grep -v '^#' /etc/wayvnc/config` (key paths shown, contents not read) [capture-2 §vnc-config] | 2026-09-23 | Confirmed |
| E-034 | Whether SSH password authentication is enabled could not be read: `sshd_config.d/50-cloud-init.conf` is root-only. `KbdInteractiveAuthentication no` is set, and one authorised key is present. | Edge | `grep -HE '^(PasswordAuthentication\|…)' /etc/ssh/sshd_config /etc/ssh/sshd_config.d/*` [capture-2 §sshd-auth-posture] | 2026-09-23 | Unverified |
| E-035 | The journal is persistent: 200 MB cap, 32 MB files, 90 days, set by the drop-in `50-ibems-persistent.conf` (RM-125, 2026-09-22). Usage is 142 MB. | Edge | `journalctl --disk-usage; cat /etc/systemd/journald.conf.d/*` [§journal-usage] | 2026-09-23 | Confirmed |
| E-036 | logrotate is configured and includes `nodered` and `mosquitto`. `/var/log` totals 144 MB. | Edge | `ls /etc/logrotate.d/; du -sh /var/log` [§logrotate] | 2026-09-23 | Confirmed |
| E-037 | The service user has no crontab and `/etc/cron.d` holds only the OS scrub job, so all scheduled iBEMS work runs on systemd timers. | Edge | `crontab -l; ls /etc/cron.d` [§cron] | 2026-09-23 | Confirmed |
| E-038 | **`unattended-upgrades` is not installed**, and there is no `20auto-upgrades`. The apt-daily timers are enabled. Without the package they install nothing; that is Hypothesis, from Debian's `apt.systemd.daily` design, and was not observed. Node.js comes from NodeSource apt and Node-RED from global npm, so no package manager upgrades Node-RED on its own. | Edge | `dpkg -l unattended-upgrades` ("no packages found"); `cat /etc/apt/apt.conf.d/20auto-upgrades`; `ls /etc/apt/sources.list.d/` [capture-2 §update-policy; edge-capture-4] | 2026-09-23 | Confirmed |
| E-039 | Of the last 100 error-priority journal entries, none comes from an iBEMS unit. All are OS noise (udev, blkmapd, bluetoothd, wpa_supplicant bgscan). | Edge | `journalctl -p err -n 100` [§recent-errors] | 2026-09-23 | Confirmed |
| E-040 | Node-RED logs a socket timeout from the `solarman-device` config node to the inverter logger about every 10 min. The "Deye Solar Inverter" tab is empty. | Edge + Flow | `journalctl -u nodered --since '-30 min'` [capture-2 §nodered-recent-journal]; tab inventory (E-050) | 2026-09-23 | Confirmed |
| E-041 | **Hardware dispatch is enabled.** `server/.env` has `HARDWARE_DISPATCH_ENABLED=true`, and the scheduler logged `dispatch=OPEN` at its start at 2026-09-23 21:06:59. The proxy logs no gate line at start, and its `/api/capabilities` answers 401 without a session. | Edge | `grep '^HARDWARE_DISPATCH_ENABLED=' server/.env`; `journalctl -u ibems-scheduler --since 2026-09-23 \| grep dispatch=` [§dispatch-flag-*, capture-3 §scheduler-latest-start, capture-2 §proxy-*] | 2026-09-23 | Confirmed |
| E-042 | `server/.env` on the edge declares 13 names: `BREAK_GLASS_PASSWORD_HASH`, `HARDWARE_DISPATCH_ENABLED`, `LIGHT_API_TOKEN`, `NODE_RED_ADMIN_PASS`, `NODE_RED_ADMIN_USER`, `NTFY_TOPIC`, `PROXY_PORT`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_URL`, `TUYA_ACCESS_ID`, `TUYA_ACCESS_SECRET`, `TUYA_REGION`, `VITE_SUPABASE_ANON_KEY`. | Edge | `grep -v '^#' server/.env \| cut -d= -f1` (names only) [§server-env-names-only] | 2026-09-23 | Confirmed |
| E-043 | `server/data/` on the edge holds `device-credentials.json` (mode 0600), `lan-map.json`, `jwks.json` and `fleet-recover.json`. | Edge | `ls -la server/data` [§server-data-listing] | 2026-09-23 | Confirmed |
| E-044 | The Node-RED user directory holds `flows.json` (382 KB, 2026-09-23 21:06), `flows_cred.json`, about 45 `flows.json.bak-*` files (7 of them root-owned, from July) and `settings.js` with 3 `.bak` copies. **All of them are on the same SD card.** | Edge | `ls -la ~/.node-red` [§node-red-userdir-listing] | 2026-09-23 | Confirmed |
| E-045 | No documented backup of `flows_cred.json`, the Node-RED `credentialSecret` or `server/data/device-credentials.json` leaves the edge. `backup-policy.md` covers Supabase only and says secrets are not exported. `~/backups` is on the same card. | Repo + Edge | `docs/backup-policy.md` §"Secrets are not exported"; `ls ~/backups` [capture-2 §backup-artifacts] | 2026-09-23 | Confirmed (docs) · Hypothesis (no copy exists anywhere) |
| E-046 | Node-RED context is on `localfilesystem`, 22 MB in total. The bridge tab's `flow.json` alone is 3.7 MB and was modified today. | Edge | `du -sh ~/.node-red/context; find ~/.node-red/context -maxdepth 2 -type f -printf …` [capture-2 §context-store-keys] | 2026-09-23 | Confirmed (size) · Hypothesis (write load on the card) |
| E-047 | On loopback, the bridge answers `GET /api/devices` and `/api/readings/latest` with 200 and no credential. Through the proxy, the same paths and `/api/capabilities` return 401 without a session. | Edge | `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:{1880,8080}<path>` [capture-2 §bridge-endpoints-*, §proxy-endpoints-*] | 2026-09-23 | Confirmed |

## Live flow (L3, X2)

| ID | Claim | Source | Command or file | Date | Status |
|---|---|---|---|---|---|
| E-050 | The live flow has 303 nodes across 6 tabs: "Energy Monitoring - Set time" 43, "Outlet" 108, "Switch" 84, "Aircon" 30, "Deye Solar Inverter" 0, and "iBEMS Bridge (Stage 1, read-only)" 28. The rest are config nodes. | Flow | tab inventory [edge-capture §flow-size-and-tabs; flow-analysis] | 2026-09-23 | Confirmed |
| E-051 | 19 `tuya-smart-device` nodes: 3 CT meters (v3.5), 7 outlets (v3.4), 7 light switches (v3.5), the IR hub (v3.3) and a sensor node, "Outside Temp" (v3.3, no address set). All have `findTimeout` 10000 ms and `retryTimeout` 1000 ms. | Flow | per-node fields [flow-analysis §Tab …] | 2026-09-23 | Confirmed |
| E-052 | **All 42 nodes the generator emits (`bridge-flow.json`) are in the live flow, with zero non-layout field differences.** 29 are on the bridge tab and 13 are the per-tab collectors plus the WebSocket config node. | Flow + Repo | node-by-node comparison of `bridge-flow.json` with the redacted live flow | 2026-09-23 | Confirmed |
| E-053 | The committed `node-red-bridge/live-flow-baseline.json` is stale: 271 nodes against 303 live. 35 live nodes are missing from it, 3 of its nodes are gone, and it has 283 field differences, 18 of them `deviceIp`. | Flow + Repo | comparison with the redacted live flow [flow-analysis §Committed …] | 2026-09-23 | Confirmed |
| E-054 | The live flow has **no `catch` nodes** and no `complete` nodes. It has 8 `status` nodes, 5 disabled nodes and no subflows. | Flow | node-type counts [flow-analysis header] | 2026-09-23 | Confirmed |
| E-055 | HTTP-in endpoints: `GET /api/devices`, `/api/readings/latest` and `/api/readings/history` on the bridge tab; `POST /capability/:deviceId` on the Energy tab; `POST /outlet/:target`, `POST /light/:id` and `POST /acu` on their tabs. | Flow | http-in inventory [flow-analysis] | 2026-09-23 | Confirmed |
| E-056 | Cadences: a 60 s GET poll per device class (meters, outlets, switches, IR hub gate), a 180 s "Trigger Every Set Mins" into Sheets, a 2 s WebSocket push, a 60 s history sample and a midnight reset (`00 00 * * *`). | Flow | inject inventory [flow-analysis] | 2026-09-23 | Confirmed |
| E-057 | Node-RED's own schedule arrays (`sched_1..7`, `outlet_sched_1..7`, `ac_sched`) are all empty, and no node in the flow writes them. | Flow + Edge | context-key map [flow-analysis §Context keys]; lengths from `~/.node-red/context/*/flow.json` [data-audit-2 §Node-RED …] | 2026-09-23 | Confirmed |
| E-058 | The live flow has 11 enabled `GSheet` append nodes (4 meter, 7 outlet) and one `gauth` config node. Each appends on the 180 s trigger. | Flow | node-type counts [capture-3 §flow-node-types; flow-analysis] | 2026-09-23 | Confirmed |
| E-059 | The per-tab flow context holds each device's parsed state (`<dev>_last_{v,c,p}`, `_energy`, `_health`, `_dp`). The bridge tab reads it through generated collectors, because flow context is scoped to one tab. | Flow + Repo | context-key map [flow-analysis]; header of `node-red-bridge/build-flow.mjs` | 2026-09-23 | Confirmed |

## Repository (all layers)

| ID | Claim | Source | Command or file | Date | Status |
|---|---|---|---|---|---|
| E-060 | Frontend: React 19.2, Vite 8, TypeScript 5.9 and Tailwind 4, with zustand 5 (16 stores in `src/stores/` plus a retry helper), recharts 3, three 0.185 and pdfmake 0.3. There is no router library. The URL hash is the route, read by a hand-written hook (`src/lib/useHashRoute.ts`). The pages are `overview`, `analytics`, `control`, `devices`, `automation`, `reports` and `settings`, behind a login gate. | Repo | `package.json`; `src/App.tsx` (`useHashRoute(ROUTE_IDS, 'overview')`); `src/lib/useHashRoute.ts`; `src/components/layout/navItems.ts`; `ls src/stores` | 2026-09-24 | Confirmed |
| E-061 | Bridge addresses are resolved only in `src/config/bridge.ts`. Over HTTPS the page uses same-origin `/api` (the mesh Serve path), otherwise the proxy on 8080. Port 1880 is used only when no database is configured. | Repo | `src/config/bridge.ts` | 2026-09-23 | Confirmed |
| E-062 | `node-red-bridge/build-flow.mjs` generates `bridge-flow.json` from `shared/registry.mjs`: the bridge tab plus a three-node collector on each of the four source tabs. `deploy.mjs` is a dry run by default. It **appends** to the live flow on `--apply`, and `--force` replaces only nodes it previously emitted. There is no "standalone" variant. | Repo | `node-red-bridge/build-flow.mjs` header; `node-red-bridge/deploy.mjs`; `git grep -i standalone` | 2026-09-23 | Confirmed |
| E-063 | The device registry is `shared/registry.mjs`, fed by `shared/sites/<site>/{site,devices,circuits}.mjs`. The active site is chosen in `shared/siteConfig.mjs`. | Repo | the files named | 2026-09-23 | Confirmed |
| E-064 | Schema: `supabase/schema.sql` plus 47 `phase*.sql` migrations, applied in filename order. They define 23 tables: `acu_loop_state`, `acu_rules`, `anomalies`, `building_totals`, `building_totals_hourly`, `commands`, `device_config`, `devices`, `dsm_thresholds`, `emission_factors`, `energy_tariffs`, `ingestion_health`, `monthly_building_reports`, `monthly_reports`, `period_building_reports`, `period_reports`, `readings`, `readings_hourly`, `schedules`, `site_ui_prefs`, `sites`, `socket_config`, `space_nodes`. | Repo | `grep -iE 'create table' supabase/*.sql` | 2026-09-23 | Confirmed |
| E-065 | `server/auditedDispatch.mjs` is the one "record, then act" path, shared by `proxy.mjs` and `scheduler.mjs`. The audit row is written **before** dispatch ("no row, no dispatch"). The gate is `HARDWARE_DISPATCH_ENABLED`. A row whose final update fails stays at `dispatching`. | Repo | `server/auditedDispatch.mjs` header | 2026-09-23 | Confirmed |
| E-066 | Authentication: Supabase Auth with email and password (`src/stores/authStore.ts`). The proxy verifies sessions offline against cached signing keys (`server/jwksCache.mjs`, `server/jwtVerify.mjs`). A break-glass login is keyed on `BREAK_GLASS_PASSWORD_HASH` (`server/breakGlass.mjs`). | Repo | the files named | 2026-09-23 | Confirmed |
| E-067 | Environment names read by server-side code: `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_ANON_KEY`, `VITE_SUPABASE_ANON_KEY`, `TUYA_ACCESS_ID`, `TUYA_ACCESS_SECRET`, `TUYA_REGION`, `NODE_RED_ADMIN_USER`, `NODE_RED_ADMIN_PASS`, `LIGHT_API_TOKEN`, `HARDWARE_DISPATCH_ENABLED`, `BREAK_GLASS_PASSWORD_HASH`, `BRIDGE_HOST`, `BRIDGE_PORT`, `BRIDGE_HTTP_URL`, `BRIDGE_TIMEOUT_MS`, `PROXY_PORT`, `INGEST_POLL_MS`, `INGEST_DEVICE_SYNC_MS`, `INGEST_BUFFER_PATH`, `INGEST_RETENTION_DAYS`, `COMMAND_AUDIT_BUFFER_PATH`, `SCHEDULER_AUDIT_BUFFER_PATH`, `SCHEDULE_TICK_MS`, `SCHEDULE_REFRESH_MS`, `SHED_OFFICE_START`, `SHED_OFFICE_END`, `SHED_DAYS`, `LAN_PRESENCE`, `JWKS_CACHE_PATH`, `DEVICE_CREDENTIALS_PATH`, plus `NTFY_SERVER` and `NTFY_TOPIC` (from the template). The browser reads `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `VITE_BRIDGE_HTTP_URL`, `VITE_BRIDGE_WS_URL` and `VITE_WEATHER_{LAT,LON,PLACE}`. | Repo | `grep -rhoE 'process\.env\.[A-Z_]+' server shared node-red-bridge scripts`; `grep -rhoE 'import\.meta\.env\.[A-Z_]+' src`; `server/.env.example` | 2026-09-23 | Confirmed |
| E-068 | CI (`.github/workflows/ci.yml`) runs on Node 22 and 24: `npm ci`, lint, build, vitest, `test:bridge`, `test:server`, then an assertion that `server/data` is clean. A separate warn-only job flags code changes that do not update `ROADMAP.md`. | Repo | `.github/workflows/ci.yml` | 2026-09-23 | Confirmed |
| E-069 | Tests are vitest for the frontend and `node --test` for the bridge and server. There is no mocking library: server tests spawn real processes. | Repo | `package.json`; `CLAUDE.md` §Conventions | 2026-09-23 | Confirmed |
| E-070 | Retention: `readings` and `building_totals` keep 30 days at per-minute resolution, then roll into permanent hourly buckets in one transaction (`roll_up_and_prune_readings`). `anomalies` keeps 365 days. `commands` is never pruned. It is triggered statelessly from the ingest daemon. | Repo | `server/retention.mjs` header; `supabase/phase9_readings_hourly.sql`, `phase11_totals_retention.sql` | 2026-09-23 | Confirmed |
| E-071 | Automation: the Automation page writes `schedules` to the database. `server/scheduler.mjs` fires them through the audited path (15 s tick, 60 s refresh) and also runs demand auto-shed and the aircon room-target loop. | Repo + Edge | `server/scheduler.mjs`; `ibems-scheduler` start line (`schedulable=15 … tick=15000ms`); `commands.source` values (E-084) | 2026-09-23 | Confirmed |
| E-072 | The documents that code or tests reference by path are `bridge-contract.md`, `storage-contract.md`, `pi-session-brief.md`, `physical-install.md`, `replication.md`, `backup-policy.md`, `outage-recovery.md`, `phase-f-runbook.md` and `adr-002-device-recovery-path.md`. Tests **read** `CLAUDE.md`, `CONTRIBUTING.md`, `docs/pi-session-brief.md` and `docs/physical-install.md`. | Repo | `grep -rnoE 'docs/[a-z0-9-]+\.md' test server scripts shared node-red-bridge src .github`; `test/service-restart-map.test.mjs`, `test/site-branch-wiring.test.mjs` | 2026-09-23 | Confirmed |
| E-073 | `npm run preflight` checks whether the **bridge** is exposed (`bridge_not_exposed`). It has no equivalent check for the broker. | Repo | `scripts/preflight.mjs` (the `bridge_not_exposed` block; no `1883` check) | 2026-09-23 | Confirmed |
| E-074 | The ingest daemon reads the bridge directly at `BRIDGE_HTTP_URL`, which defaults to Node-RED on loopback (`http://127.0.0.1:1880/api`). The browser never does: it goes through the proxy (E-061, E-047). | Repo | `server/ingest.mjs` (the `BRIDGE_URL` constant) | 2026-09-23 | Confirmed |
| E-075 | Prisma, Render, Modbus, BACnet, TimescaleDB, Home Assistant and "predictive" appear in **no** tracked file other than `ROADMAP.md`. `n8n` appears only in the flow-cleanup plan and its test, which record it as "trialled and set aside; it is not installed". | Repo | `git grep -ilE '<term>' -- ':!ROADMAP.md'` per term; `node-red-bridge/cleanupPlan.mjs` | 2026-09-23 | Confirmed |
| E-079 | The operator states that a complete copy of `flows_cred.json`, the Node-RED `credentialSecret`, `server/data/device-credentials.json` and `server/.env` exists off the edge's SD card. Its location is deliberately not recorded here, and no restore from it has been performed. | Operator | GATE A answer, 2026-09-24 | 2026-09-24 | Stated |
| E-078 | When the database cannot be reached, the audit row is written durably to a local buffer (`server/auditQueue.mjs`; paths `COMMAND_AUDIT_BUFFER_PATH` and `SCHEDULER_AUDIT_BUFFER_PATH`) and dispatch proceeds. "Recorded" means durably written locally or in the database, and hardware still never moves without a record. `/api/capabilities` reports the backlog as `audit_buffer_pending`. The dispatch policy defaults to `local-first`, with the vendor cloud as an optional fallback. | Repo | `server/auditQueue.mjs` header; `server/proxy.mjs` (`DISPATCH_POLICY`, `audit_buffer_pending`) | 2026-09-23 | Confirmed |
| E-077 | The edge's desktop session is also used for general browsing. The non-kiosk Chromium profile in `~/.config/chromium` holds site data for general-purpose web sites and for the loopback port n8n used to serve (5678). | Edge | the `grep -rls chromium ~/.config` listing (paths only) [edge-capture-4] | 2026-09-23 | Confirmed |
| E-076 | Anomaly detection is rolling-window statistics with no ML or training data. A z-score check and Tukey IQR fences must **agree** before a row is written (`method: both` in the sample row). | Repo + DB | `server/anomalyStats.mjs` header; `anomalies` sample [data-audit] | 2026-09-23 | Confirmed |

## Data (L4)

Counts were taken about 15:00 UTC on 2026-09-23.

| ID | Claim | Source | Command or file | Date | Status |
|---|---|---|---|---|---|
| E-080 | Row counts: `readings` 858,691; `building_totals` 43,313; `anomalies` 8,539; `readings_hourly` 3,773; `commands` 1,727; `period_reports` 900; `building_totals_hourly` 190; `period_building_reports` 45; `schedules` 23; `devices` 20; `monthly_reports` 20; `device_config` 14; `socket_config` 14; `space_nodes` 4; one row each in `acu_loop_state`, `acu_rules`, `dsm_thresholds`, `ingestion_health`, `monthly_building_reports`, `site_ui_prefs` and `sites`; `energy_tariffs` and `emission_factors` empty. | DB | `GET /rest/v1/<t>?select=*&limit=1` with `Prefer: count=exact` [data-audit] | 2026-09-23 | Confirmed |
| E-081 | `readings` spans 2026-08-24 13:00 UTC to now, which is the 30-day window. At about one row per device per minute for 20 devices, that is roughly 28,600 rows a day. The hourly tables start 2026-08-16. | DB | span query per table [data-audit] | 2026-09-23 | Confirmed |
| E-082 | In `building_totals`, the median interval is 60 s. The largest gap in the retained window is 600 s (2026-08-26 02:29 to 02:39 UTC), and it is the only gap over 300 s. **A gap is absent rows, not zero-filled rows.** `readings` holds 7 rows inside that gap. | DB | paged `select=ts&order=ts.asc` over all rows [data-audit §gaps] | 2026-09-23 | Confirmed |
| E-083 | `readings` has 147,364 rows with `online=false`. Of those, 94,457 carry `power_w` null and **36,041 carry `power_w = 0`** (5,389 in the last 7 days). Aggregates count only online samples (`storage-contract.md`). | DB + Repo | filtered counts [data-audit-2, data-audit-3]; `docs/storage-contract.md` | 2026-09-23 | Confirmed |
| E-084 | `commands` has 1,727 rows since 2026-08-17. The first `dispatched` row is 2026-08-24. By source: `dsm_autoshed` 953, `ibems-app` 541, `schedule` 228, `acu_loop` 5. **19 rows remain at `dispatching`**, all `dsm_autoshed`; the latest is 2026-09-23 04:52 UTC. | DB | paged `select=status,source,via,requested_at` [data-audit-2, data-audit-3] | 2026-09-23 | Confirmed |
| E-085 | `ingestion_health`: the last success is within the minute, `buffered_row_count` is 0 and `scrub_rejected_count` is 0. | DB | [data-audit] | 2026-09-23 | Confirmed |
| E-086 | The ingest daemon logs `wrote 20 readings + totals` about every 60 s. | Edge | `journalctl -u ibems-ingest --since '-30 min'` [capture-2 §ingest-recent-journal] | 2026-09-23 | Confirmed |
| E-087 | `schedules` has 23 rows, of which 1 is enabled (the aircon). `dsm_thresholds` has `auto_shed` on with both limits set. These settings belong to the operator. | DB | [data-audit-2 §schedules; data-audit] | 2026-09-23 | Confirmed |
| E-088 | `sites` has one row: time zone Asia/Manila, UTC offset 480 min. Timestamps are stored as `timestamptz` and returned in UTC. | DB | [data-audit §sites] | 2026-09-23 | Confirmed |
| E-089 | Supabase's Free plan has a 500 MB database and **goes read-only above it**. It pauses after 1 week of inactivity and includes no automatic backups. The Pro plan includes 8 GB, keeps daily backups for 7 days and never pauses. | Web | supabase.com/pricing; supabase.com/docs/guides/platform/database-size | 2026-09-23 | Confirmed |
| E-090 | Which plan this project is on, and its database size, are recorded nowhere in the repository and cannot be read over REST. | Repo + DB | `grep -iE 'free (plan\|tier)\|500 ?MB\|pro plan' ROADMAP.md docs/*.md` | 2026-09-23 | Unverified |

## Handbook port (S2)

These rows are the evidence behind each audit-driven change in `ported-changes.md`.

| ID | Claim | Source | Command or file | Date | Status |
|---|---|---|---|---|---|
| E-110 | The "late" budget is **per device class, 2.5 × the measured reporting cadence**: 150 s for outlets, meters and the IR-driven aircon (polled every 60 s), 30 s for switches and the room sensor, whose readings are stamped at collection. A single 30 s budget made every outlet flicker to "stale" twice a minute. A device may override its class. | Repo | `shared/registry.mjs` (`STALE_AFTER_MS_BY_CLASS` and its rationale); `src/lib/staleness.ts` (`staleWindowMs`) | 2026-09-24 | Confirmed |
| E-111 | A reading's figures are withdrawn after 300 s (`EXPIRED_AFTER_MS`). The bridge marks a device `online: false` after 600 s with no new arrival (`STALE_READING_MS`), which drops it from building totals. | Repo | `src/lib/staleness.ts`; `shared/buildLatest.mjs` | 2026-09-24 | Confirmed |
| E-112 | The aircon's hard command range is **16–30 °C**, the degrees the IR library holds codes for, identical at every site. The room-target loop's defaults are a 0.5 °C deadband, 1 °C steps and `min_step_interval_s` 600. It raises `floor_reached`, `ceiling_reached`, `sensor_unavailable` or `target_below_policy`. A missing or stale sensor reading produces no step. | Repo | `shared/commands.mjs` (`ACU_MIN_C`, `ACU_MAX_C`); `supabase/phase36_acu_rules.sql` (column defaults); `shared/acuLoopVocabulary.mjs`; `server/acuLoopPlan.mjs` | 2026-09-24 | Confirmed |
| E-113 | Site policy holds two floors, separate from the hardware range: the coldest setpoint a person may request (`acu_min_setpoint_c`) and the coldest room target a rule may aim for (`acu_min_room_target_c`). Both are 24 °C at the pilot. The pilot's one loop rule steps every 300 s. | DB + Repo | `GET sites?select=policy`; `shared/sitePolicy.mjs`; `acu_rules` (E-087) | 2026-09-24 | Confirmed |
| E-114 | Handbook Ch.2's test manual contains **33 tests**: 6 control, 6 reliability, 5 scheduling, 10 automation, 6 data logging. They are held in the page's `SUITES` data. The "0 / 29" counter and "A 29-test commissioning manual" are stale static text. | Workspace | `SUITES` literal extracted from `iBEMS-Handbook-02-Control-Strategy.html` [handbook-02-data.json] | 2026-09-24 | Confirmed |
| E-115 | The browser verifies a command by reconciling each pending command against the readings that follow it. If the device's reported state never changes, it says "The device did not report the new state." The audit row itself records dispatch, not confirmation (E-084). | Repo | `src/stores/commandStore.ts` (`reconcile`) | 2026-09-24 | Confirmed |
| E-117 | **A ping with no reply is not evidence that a network isolates clients.** Windows Firewall drops ICMP echo by default. The reliable check is whether the neighbour table (ARP) resolves the other host's MAC. This is recorded from two wrong diagnoses in August 2026. | Repo | `CLAUDE.md` §Site facts ("A ping that gets no reply …") | 2026-09-24 | Confirmed (recorded incident) |
| E-118 | **A device that looks unreachable may be a controller node that has given up.** A Node-RED restart reconnected a device diagnosed as an RF fault within 2 s (2026-08-25), and took the fleet from 9/21 to 14/21 online. `ibems-fleet-recover.timer` now automates the restart when devices are reachable but not connected (E-022). | Repo + Edge | `CLAUDE.md` §Site facts; `server/fleet-recover.mjs`; E-022 | 2026-09-24 | Confirmed |
| E-119 | **The device driver never reads a device's state on connect; only a poll does.** A device nothing polls shows its last pushed value indefinitely, across restarts. The meters held a stale 39.8 W for hours on 2026-09-22 until they were polled (RM-134). Every device node is now polled every 60 s (E-056), and `preflight` fails when one is not. | Repo + Flow | `CLAUDE.md` §Site facts; E-056; `scripts/preflight.mjs` (`flow_polls`) | 2026-09-24 | Confirmed |
| E-120 | **A manual aircon setpoint below the site's policy floor is accepted, with a warning recorded in the command's audit note. It is not refused.** Only the hardware range (16–30 °C) refuses. The policy floor now means the coldest *room target* a rule may aim for. | Repo | `shared/commands.mjs` (the comment above `setpointPolicyWarning`); `server/proxy.mjs` (warnings folded into the audit note) | 2026-09-24 | Confirmed |
| E-121 | **A rule aimed below the room-comfort policy is refused unless a written reason of at least 10 characters is recorded** ("permitted, but only on the record"). A rule saved or armed without a signed-in user is refused. Schedules must carry `updated_by` (a check constraint). | Repo | `supabase/phase36_acu_rules.sql` (`upsert_acu_rule`); `supabase/phase33_schedules_stackable.sql` (`schedules_updated_by_present`) | 2026-09-24 | Confirmed |
| E-122 | The audit row is written **before** the gate is acted on, and it carries the gate's decision: a closed gate or a non-dispatchable class opens the row as `dry_run`, an open gate as `dispatching`. Dispatch happens only after the row exists. | Repo | `server/auditedDispatch.mjs` (`willDispatch`, `openingStatus`, `insertAudit` before `dispatch`) | 2026-09-24 | Confirmed |
| E-123 | Auto-shed **sheds only and never restores**, evaluates **one tier per pass**, and never sheds a relay that is unassigned or marked `never`. Since RM-067 the unit is a socket, not a device. | Repo | `server/shedPlan.mjs` header | 2026-09-24 | Confirmed |
| E-124 | **A database restore was performed on 2026-09-22** with `npm run restore:rehearse`: 19 tables and 12,960 rows went into a throwaway PostgreSQL 16, and every count and every row matched the export. The scratch-Supabase-project half was deliberately not exercised. `docs/backup-policy.md`'s status banner still said "no restore has been performed" until S3 corrected it. | Repo | `ROADMAP.md` RM-006d; `docs/backup-policy.md` (banner before 2026-09-24) | 2026-09-24 | Confirmed (recorded) |
| E-125 | The pilot's stand-alone temperature/humidity node ("Outside Temp") was **never installed**. It is quiesced and reports `online: false`. The room's temperature and humidity come from the IR hub's own data points, the hub being a Commander that also senses. | Repo + Flow | `docs/pi-session-brief.md` §Standing facts ("Outside Temp"); `CLAUDE.md` §Site facts (the IR hub's dps); E-051 (no address set) | 2026-09-24 | Confirmed |
| E-116 | Handbook Ch.1's 12 catalogue entries (function blocks, triggers, settings, warnings) live in the page's `DEVICES` data, not in its static HTML. | Workspace | `DEVICES` literal extracted from `iBEMS-Handbook-01-Field-Devices.html` [handbook-01-devices.json] | 2026-09-24 | Confirmed |

## Overview and edge chapters (Phase B)

| ID | Claim | Source | Command or file | Date | Status |
|---|---|---|---|---|---|
| E-126 | No migration creates a row-level-security policy for the `anon` role. `phase39` revokes raw telemetry tables (`readings`, `readings_hourly`, `building_totals`, `building_totals_hourly`) from `anon` and `authenticated`, so history is read through database functions, not by selecting raw tables. | Repo | `grep -ciE 'create policy[^;]*to anon' supabase/*.sql` (no file with a non-zero count); `supabase/phase39_privilege_lockdown.sql`; `docs/storage-contract.md` §Reading history | 2026-09-24 | **Superseded by E-161**: the grep was single-line; the conclusion holds, the wording did not |
| E-127 | **One transform, two consumers.** The real bridge and the mock bridge build readings with the same shared module (`shared/buildLatest.mjs`). A contract test holds the mock to a pure, in-process simulation: no vendor client, no broker, no socket and no outbound request, and anything it borrows from `server/` must have no imports of its own. | Repo | `test/contract.test.mjs` (the mock-purity test and the `buildLatest` read) | 2026-09-24 | Confirmed |
| E-128 | **Design rules as coded:** <br>• A database 4xx is an answer and still refuses; only a transport failure is buffered (`server/proxy.mjs`, `server/auditQueue.mjs`). <br>• Enrolment writes the registry first and removal writes the flow first, so that neither leaves hardware polled but unlisted (`server/enrollService.mjs`, `server/removeService.mjs`). <br>• Fleet alarms are edge-triggered (`server/fleetAlarm.mjs`). <br>• Commands are absolute, never "toggle" (`shared/commands.mjs`). <br>• Break-glass sessions are view-only (`server/proxy.mjs`). <br>• Every live-flow script defaults to a dry run (`node-red-bridge/deploy.mjs` and its siblings). | Repo | the files named | 2026-09-24 | Confirmed |
| E-129 | **`scripts/install.sh`** is a dry run by default and idempotent, and never overwrites `server/.env`. <br>• **Steps:** preflight; Node 22 via NodeSource if the installed Node is outside `>=22 <25`; the application and `serve`; Node-RED plus `node-red-contrib-tuya-smart-device`; Mosquitto with a loopback-only listener, only if newly installed; an empty `server/.env`; the four system units, rewritten for the running user and checkout; start the dashboard and enable the other three. <br>• **It deliberately never** touches Wi-Fi, writes secrets or deploys the flow. <br>• **It does not install** the kiosk user unit, the Wi-Fi, LAN-map or fleet-recovery timers, the journald drop-in, the Node-RED log rate limit, or Node-RED's `settings.js` hardening. <br>• **Tested:** the apply path only in a container with `systemctl` stubbed. It "has never been run end to end on a real machine". | Repo | `scripts/install.sh` (header and steps 1–8); `grep -c` for each unscripted item | 2026-09-24 | Confirmed |
| E-130 | **`npm run preflight`** reports these checks: `env_supabase`, `env_tuya`, `env_tuya_region`, `env_node_red_admin`, `db_reachable`, `db_site_row`, `vendor_auth`, `network_discovery`, `bridge_reachable`, `bridge_not_exposed`, `bridge_fleet`, `flow_polls`, `host_journal`, `host_timers` and `host_addresses`, plus a service-state check. A check that could not run is reported as unchecked, never as passed. It has no broker, `adminAuth` or `credentialSecret` check. | Repo | `scripts/preflight.mjs` | 2026-09-24 | Confirmed |
| E-131 | **Deploying is two separate acts.** <br>• A change under `server/` or `shared/` needs `ibems-ingest`, `ibems-proxy` and `ibems-scheduler` restarted, because Node loads a module once. <br>• `src/` changes, and the `shared/` modules `src/` imports, need `npm run build`, because the kiosk is served from `dist/`. <br>• On 2026-09-13 the ingest daemon was found still running modules replaced four days earlier. <br>• The restart map lives in `docs/pi-session-brief.md`, and `test/service-restart-map.test.mjs` derives it from the unit files. | Repo | `CLAUDE.md` §Running it; `test/service-restart-map.test.mjs` | 2026-09-24 | Confirmed |
| E-132 | **Two per-device settings live only on the live flow.** <br>• `findTimeout` and `tuyaVersion` sit on the hand-built tabs, which the generator does not own. <br>• `deploy:pi` appends and never rewrites them, and nothing in the repo *declares* them, so a restored old flow or a hand edit loses them silently. <br>• The fleet broadcasts every 5.0 s, which is why the timeout is 10,000 ms. <br>• Devices speak protocol v3.3–v3.5, and a node declaring the wrong version fails like a network fault. | Repo + Flow | `CLAUDE.md` §Site facts; E-051 (the values live) | 2026-09-24 | Confirmed |
| E-133 | **Node-RED was bound to every interface until 2026-09-01.** That exposed the admin API and every http-in node to the device network, and was measured from another host. It has been `uiHost: "127.0.0.1"` since then, and the editor is reached through an SSH tunnel. | Repo + Edge | `CLAUDE.md` §Site facts; E-019, E-025 | 2026-09-24 | Confirmed |
| E-134 | **Ingest buffered through a real outage.** A ~7-minute uplink loss on 2026-08-21 buffered 8 rows locally, and they flushed on reconnect with no data loss (`buffered_row_count` back to 0). | Repo | `ROADMAP.md` (Server & ingestion, the ingest-buffer entry) | 2026-09-24 | Confirmed (recorded) |
| E-135 | **Datapoint scaling is from the capability catalogue, per product:** `value ÷ 10^scale × unit factor`. A CT meter's `cur_power` is scale 1 (W), `cur_voltage` scale 1 (V) and `cur_current` scale 3 (A). An outlet's current is scale 0 in mA. Example: the stored reading `227 V / 7.692 A / 815.9 W` (E-080 sample). | Repo + DB | `shared/deviceCapabilities.mjs` (`cap()` entries and the scale comment); E-080 | 2026-09-24 | Confirmed |
| E-137 | Timestamps are stored in UTC (`timestamptz`, E-088). The UI formats any moment that describes the **building** (a reading, a device's last report, a forecast day) in the **site's** time zone rather than the reader's. Before RM-033 this had been missed in 16 places, because the kiosk in the room was always right. | Repo | `src/lib/siteTime.ts` header | 2026-09-24 | Confirmed |
| E-138 | **Command and ingest details as coded:** <br>• The proxy acknowledges an accepted command with **HTTP 202** and `confirmed: false, confirmation: 'none'` (`ACCEPTED_STATUS = 202`). <br>• `meter` and `sensor_temp_humidity` are **not commandable** (`NOT_COMMANDABLE_CLASSES`). <br>• Ingest's scrub **stores a non-finite or out-of-bounds value as NULL** and counts a rejection. It **drops a row only when its timestamp is unusable**. Rows replayed from the outage buffer bypass it. <br>• The edge-triggered fleet alarm is sent from the ingest cycle. | Repo | `shared/commands.mjs`; `server/proxy.mjs`; `server/scrubTelemetry.mjs`; `server/ingestCycle.mjs` (imports `fleetAlarm`) | 2026-09-24 | Confirmed |
| E-139 | **SD-card write reduction on the edge:** `noatime` on `/` (E-012), swap on zram (E-011), `fstrim.timer` and `e2scrub_all.timer` enabled, the journal capped at 200 MB (E-035), logrotate (E-036), and a Node-RED journal rate limit (E-020). The rate-limit drop-in is in the repo (`server/nodered-log-ratelimit.conf`). **The persistent-journal drop-in (`50-ibems-persistent.conf`) is not**: only `preflight` (`host_journal`) and ROADMAP name it. | Edge + Repo | `systemctl list-unit-files --state=enabled` [edge-capture §unit-files-enabled]; `git grep -l '50-ibems-persistent\|Storage=persistent'` (ROADMAP, preflight, this ledger only) | 2026-09-24 | Confirmed |
| E-140 | **Node-RED encrypts flow credentials (`flows_cred.json`) with `credentialSecret`.** When none is set it uses a system-generated key. If that key is lost, the credentials file cannot be recovered and must be deleted and the credentials re-entered. Node-RED recommends setting your own `credentialSecret` in `settings.js`. `uiHost` defaults to `0.0.0.0` (all IPv4 interfaces). | Web | nodered.org/docs/user-guide/runtime/configuration (`uiHost`); Node-RED's own start-up warning, quoted in its Docker guide and forum (nodered.org/docs/getting-started/docker; discourse.nodered.org) | 2026-09-24 | Confirmed (vendor docs) |
| E-141 | **The proxy refuses to start** when `HARDWARE_DISPATCH_ENABLED=true` and `LIGHT_API_TOKEN` is unset. The source calls that combination "a dangerous half-configuration, not a safe degraded mode". | Repo | `server/proxy.mjs` (the start-up guard) | 2026-09-24 | Confirmed |
| E-142 | **Ingest rejects a reading timestamp more than 5 min in the future or more than 7 days in the past** (`TS_MAX_FUTURE_MS`, `TS_MAX_PAST_MS`). Rows replayed from the outage buffer bypass this check. | Repo | `server/scrubTelemetry.mjs` | 2026-09-24 | Confirmed |
| E-143 | **When Node-RED is not yet listening, the proxy logs the failed upstream call.** At the 2026-09-22 boot: `upstream GET /api/devices failed: Error: connect ECONNREFUSED` (loopback, port 1880). What the browser shows then was not observed. | Edge | `journalctl -u ibems-proxy -b` [edge-capture-2 §proxy-startup-journal] | 2026-09-24 | Confirmed (log) · UI effect Unverified |
| E-144 | **Raspberry Pi 4 Model B power:** USB-C, 5 V at 3 A (15 W). Unlike the Raspberry Pi 5, the Pi 4 has no RTC connector. | Web | raspberrypi.com/documentation/computers/raspberry-pi.html (specification table) | 2026-09-24 | Confirmed (vendor docs) |
| E-145 | **Local keys come from a key tool's export** (JSON or CSV). They load through Add Device → Import keys or `npm run keys:import`, a dry run until `--apply`. Keys are never printed, only their length. They are stored at mode 0600 in the proxy's credential store, which the proxy re-reads on every request, with no restart. Delete the export afterwards. **A local key changes only when a device is re-paired**, not on a restart. A re-paired device is pointed at its new identity with `rebind:pi`. | Repo | `server/import-keys.mjs` header; `CLAUDE.md` §Site facts; `docs/pi-session-brief.md` (the commands that matter) | 2026-09-24 | Confirmed |
| E-146 | **The proxy listens passively for device announcements on the LAN.** A device paired minutes ago appears within about 5 s, with its id, product key and protocol version, and no vendor cloud involved. A flow node whose device has not been heard for minutes is local evidence that it was re-paired or has left the network. | Repo | `server/lanPresence.mjs` header | 2026-09-24 | Confirmed |
| E-147 | **Two logical meters can share one physical dual-channel device, and the device itself can swap which clamp it reports under which channel.** This was observed 2026-09-19 and 2026-09-21. Identity is the *logical* meter, never the vendor's device id. The demux assigns channels from physical facts the operator stated, carries the decision between samples, and never guesses. | Repo | `shared/channelDemux.mjs` header; `docs/physical-install.md` §2 | 2026-09-24 | Confirmed |
| E-148 | **`npm run site:check` refuses** duplicate device ids, duplicate flow-context prefixes and duplicate state keys. Any of them would make two devices overwrite each other's readings, and the dashboard would show one device twice. | Repo | `scripts/site-check.mjs` | 2026-09-24 | Confirmed |
| E-149 | **Each building is a site.** Every table carries a `site_id` (phase19, phase20, RM-027). A second building is a sibling directory, `shared/sites/<slug>/`, selected by one line in `shared/siteConfig.mjs`, and nothing else in the codebase names a building. | Repo | `supabase/phase19_sites.sql`, `phase20_site_scoping.sql`; `shared/sites/<site>/site.mjs` header | 2026-09-24 | Confirmed |
| E-150 | **A current transformer with an open secondary on a live conductor can develop dangerous voltage.** Its shorting links stay on until it is wired to the meter. A CT clamped backwards reads negative or zero, and its arrow (K→L) faces the load. | Repo | `docs/physical-install.md` §0, §2 | 2026-09-24 | Confirmed (recorded; not reviewed by an electrician, per that file) |
| E-151 | **The IR commander:** <br>• The hub never echoes an IR send (`shouldWaitForResponse: false`, RM-144). <br>• Where the unit's IR protocol decodes, the flow builds any state from one captured frame, and refuses to install that generator unless it rebuilds every captured code exactly. <br>• Captured frames, and OFF, are always sent as captured. <br>• Without a decodable protocol, mode, fan and swing need the vendor's virtual remote in the cloud. | Repo | `CLAUDE.md` §Site facts (the aircon entries); `shared/irTcl112.mjs`; `node-red-bridge/aircon-flow.mjs` | 2026-09-24 | Confirmed |
| E-152 | **A clamp that stops measuring is detected from its own energy register**, and the "frozen" flag is stored with the reading (RM-133, FI-027). A value that survives a poll is the device's own, not the bridge's. | Repo | `node-red-bridge/valueFreezeTracker.mjs`; `ROADMAP.md` RM-133, RM-134; `CLAUDE.md` §Site facts | 2026-09-24 | Confirmed |
| E-153 | **Two of the edge's four Wi-Fi profile names appeared in backticks in tracked files**: both in `docs/outage-recovery.md`, one in `ROADMAP.md` (twice). They were replaced on 2026-09-24, and a re-scan finds none, in backticks or next to "SSID". The names were matched against the edge's NetworkManager profiles and never printed. | Edge + Repo | `nmcli -t -f NAME,TYPE con show` (names kept in scratch only); exact-match scan of `git ls-files` | 2026-09-24 | Confirmed |
| E-154 | **Tailscale SSH on the edge runs in check mode.** On 2026-09-24 a read-only SSH session paused with "Tailscale SSH requires an additional check" and a browser re-authentication link, and continued once the operator approved. | Edge | the SSH session output, 2026-09-24 | 2026-09-24 | Confirmed |
| E-155 | **What needs the internet:** <br>• the hosted database: ingest, the scheduler and the proxy's audit writes, all buffered when it is unreachable (E-078, E-134) <br>• the sign-in service (sessions verify offline against a cached key, E-066) <br>• the mesh network's coordination and relays <br>• the optional vendor cloud <br>• network time (E-014) <br>• push notifications (`NTFY_TOPIC`, E-042) <br>• the dashboard's weather card, which the **browser** fetches from `api.open-meteo.com` <br>Device control needs none of these. | Repo + Edge | `src/lib` (the only outbound host literal is `api.open-meteo.com`); the rows cited | 2026-09-24 | Confirmed |
| E-156 | **Uplink use, one 60 s sample:** the edge's Wi-Fi interface sent 910 kB and received 247 kB, about 15 kB/s out and 4 kB/s in, mesh traffic included. The default route is the Wi-Fi interface, so the device network also carries the uplink. | Edge | `/proc/net/dev`, read twice 60 s apart; `ip route show default` (address removed) | 2026-09-24 | Confirmed (single sample) |
| E-157 | **Packet yield:** the daily share of expected minutes in which each device reported (`online_sample_count / expected_sample_count`, stand-alone sensor excluded) had a **median of 99.9 % on 2026-09-16 to 09-19**. It fell on outage days: median 59.6 % on 09-21 and 67.2 % on 09-22. | DB | `GET period_reports?period=eq.day&period_start=gte.2026-09-15` | 2026-09-24 | Confirmed |
| E-158 | **The edge holds four Wi-Fi profiles.** The device SSID has the highest autoconnect priority (30), the office SSID is the fallback (10), and two others sit at 0. `ibems-wifi-prefer` returns the edge to the highest priority when it is back in range (E-022). Names are not printed. | Edge | `nmcli -t -f NAME,TYPE,AUTOCONNECT-PRIORITY con show` (lengths only) | 2026-09-24 | Confirmed |
| E-159 | **Device protocol ports:** <br>• Discovery broadcasts arrive on UDP **6666** (v3.1, plaintext JSON) and **6667** (v3.3/3.4 AES-128-ECB; v3.5 AES-128-GCM). The LAN-presence listener also binds UDP **7000**. <br>• Control is TCP **6668** to each device. <br>• Broadcasts are layer-2 local and do not cross a router or VLAN. | Repo | `server/lanDiscovery.mjs` header; `server/lanPresence.mjs`; `docs/outage-recovery.md` ("accepts TCP on 6668") | 2026-09-24 | Confirmed (broadcast scope: general networking) |
| E-160 | **The mesh network's SSH policy is broader than recorded before.** On 2026-09-24 a session **as a different local user** (`nobody`, whose no-login shell answered) was accepted; the earlier note had other user names rejected. The service account has **passwordless sudo** (`sudo -n true` succeeded, 2026-09-24). The tailnet lists 7 nodes, including devices last seen 36–37 days ago (E-027). The policy file itself is in the tailnet admin console and was not read. | Edge | `ssh nobody@<edge-host> true`; `sudo -n true` on the edge; `tailscale status` [edge-capture §tailscale] | 2026-09-24 | Confirmed (policy contents Unverified) |
| E-161 | **Corrects E-126.** <br>• `phase4_anon_read.sql` created three temporary `*_select_anon` policies, on `devices`, `readings` and `building_totals`. <br>• `phase5_lockdown_rls.sql` drops all three, and no later migration creates one. <br>• So **with every migration applied in order, the `anon` role has no policy**, and `phase39` also revokes raw telemetry from it (E-126). <br>• Every other policy is scoped to `authenticated`, plus `commands_complete_own_inflight` on `commands`. <br>• The live `pg_policies` view was not read (no SQL access). | Repo | `grep -nE 'select_anon' supabase/*.sql`; `grep -hiE 'create policy' supabase/*.sql` (policy names by table) | 2026-09-24 | Confirmed (from migrations); live state Unverified |
| E-162 | **Ingest writes are idempotent upserts** (`Prefer: resolution=merge-duplicates`), with these conflict keys: <br>• `readings (device_id, ts)` <br>• `building_totals (ts)` <br>• `anomalies (device_id, ts, metric)` <br>• `devices (id)`, `ingestion_health (id)` <br>A re-sent or replayed row replaces its twin, never duplicates it. | Repo | `server/supabaseRest.mjs`; `server/ingestCycle.mjs` (`io.write` calls); `server/ingest.mjs` | 2026-09-24 | Confirmed |
| E-163 | **There is no role finer than "signed in".** No migration or auth code defines an admin or operator role, so every signed-in account may do everything the `authenticated` policies allow: schedules, thresholds, comfort rules, tariffs, device configuration. On `commands`, a user may update only **their own** row, and only while it is `dispatching` (`commands_complete_own_inflight`). | Repo | `grep` for role concepts across `supabase/*.sql`, `src/stores/authStore.ts`, `server/proxy.mjs` (none); `supabase/phase9_command_outcome.sql` | 2026-09-24 | Confirmed |
| E-164 | **Every automatic command has an owner.** <br>• Schedule commands carry the schedule's `updated_by`. <br>• Auto-shed commands carry the thresholds' `updated_by`. <br>• Aircon-loop commands carry the rule's `updated_by`. <br>"No attribution, no command", because `commands.requested_by` is NOT NULL. | Repo | `server/schedulePlan.mjs`, `server/shedPlan.mjs`, `server/acuLoopPlan.mjs`, `server/scheduler.mjs` | 2026-09-24 | Confirmed |
| E-165 | **History has two readers:** <br>• `readings_buckets` serves the per-minute window. <br>• `readings_archive` reads across the 30-day boundary into the hourly rollups (phase10, EX-032). <br>The archive used to be write-only until phase10 gave it a reader. | Repo | `src/lib/supabaseHistory.ts`; `supabase/phase9_history_buckets.sql`, `phase10_history_archive.sql` | 2026-09-24 | Confirmed |
| E-166 | **Tariffs and emission factors:** <br>• `energy_tariffs`: `currency` (3 letters), `rate_per_kwh` (> 0, ≤ 1000), `source` (required), `effective_from`; unique per site and date. <br>• `emission_factors`: `kg_co2e_per_kwh` (> 0, ≤ 2.0), `source`, `effective_from`. <br>• Both record who set them. Both are empty at the pilot (E-080). | Repo + DB | `supabase/phase38_tariff_emissions.sql`; E-080 | 2026-09-24 | Confirmed |
| E-167 | **Google Sheets limits:** 60 write requests per minute per user per project (300 per project); a request times out at 180 s; a spreadsheet holds up to 20 million cells. | Web | developers.google.com/workspace/sheets/api/limits; support.google.com/drive/answer/37603 | 2026-09-24 | Confirmed (vendor docs) |
| E-168 | **Building totals are the sum of the branch meters** (RM-057, phase32), with the legacy flow integration kept beside them in `*_integrated` columns. Every building-wide aggregate **skips a meter whose `online` is false**, and a phase with no reporting meter yields **null, not zero**. | Repo | `shared/buildLatest.mjs` (the aggregate block, `phaseCurrent`); `supabase/phase32_building_totals_summed.sql` header | 2026-09-24 | Confirmed |
| E-169 | **Public sign-up is open.** The auth service reports `disable_signup: false` with the email provider enabled. New accounts must confirm their email first (`mailer_autoconfirm: false`). | Edge → auth service | `GET /auth/v1/settings` with the public key, run on the edge; only these flags printed | 2026-09-24 | Confirmed |
| E-176 | **Sign-up is closed.** At 13:03 on 2026-09-24 the auth service reports `disable_signup: true`, after the operator turned off "Allow new users to sign up". This supersedes the open state in E-169. | Edge → auth service | the same `GET /auth/v1/settings` as E-169 | 2026-09-24 | Confirmed |
| E-170 | **Nothing narrower than "signed in" decides who may act.** <br>• Nothing in `src/` creates an account (no sign-up call), and no document says how one is made. <br>• `handleCommand` accepts any verified session that has a user id. `server/` has no account allowlist. <br>• `schedules` accepts inserts and updates from any `authenticated` session. The scheduler fires every enabled schedule that has an `updated_by` on a dispatchable device. | Repo | `server/proxy.mjs` (`handleCommand`); `supabase/schema.sql` (the `schedules_*_authenticated` policies); `server/schedulePlan.mjs` (`dueCommands`); `grep -rn signUp src` (none) | 2026-09-24 | Confirmed |
| E-171 | **The page carries the database address.** Vite inlines `VITE_SUPABASE_*` at build time, and the bundle the kiosk serves holds the database host twice. Anyone who can load the dashboard can read both the address and the public key. That includes anyone on the device segment or the mesh network (E-025, E-026). The public key is public by design, so keeping it private is not a control. | Edge | `grep -o` count of the host suffix in `dist/assets/index-*.js` (count only) | 2026-09-24 | Confirmed |
| E-172 | **Break-glass sign-in:** <br>• `POST /api/local-login` checks the password against a scrypt hash and issues an opaque session. The session lasts 12 h, in memory, and a proxy restart ends it. <br>• The proxy logs each attempt with its origin, but **not whether it succeeded**. There is **no rate limit or lockout**. <br>• Such a session can read but not command: `403 break_glass_cannot_command`. <br>• The login page offers it only after a network error reaching the account service. | Repo | `server/proxy.mjs` (`handleLocalLogin`, `LOCAL_SESSION_TTL_MS`, the `/api/local-login` route, `handleCommand`); `server/breakGlass.mjs`; `src/components/auth/LoginPage.tsx` | 2026-09-24 | Confirmed |
| E-173 | **Browser timing:** <br>• **Live feed:** pushed every 2 s over `/ws/live`. When the socket drops, the app polls every 15 s, doubling to at most 120 s, while it retries the socket. <br>• **Timeouts:** requests 10 s; the first device-list fetch 30 s, retried on the same backoff. <br>• **Commands:** time out at 5 s, get 6 s to be confirmed by the feed, and are marked failed after 30 s with no acknowledgement. <br>• **New builds:** checked every 5 min. The kiosk reloads itself after 60 s untouched; other viewers are offered a reload. <br>• **Header pill:** `LIVE`, `LIVE (POLL)`, `RECONNECTING`, `OFFLINE`. | Repo | `src/lib/timing.ts`; `src/lib/bridgeClient.ts` (`connectLive`, `nextPollDelayMs`); `src/hooks/useLiveConnection.ts`; `src/stores/commandStore.ts` (`reconcile`); `src/hooks/useBuildWatch.ts`; `src/components/layout/TopNav.tsx` | 2026-09-24 | Confirmed |
| E-174 | **The app without a database**, run on the mock bridge with `VITE_SUPABASE_*` blanked, asks for no sign-in and says what it cannot do: <br>• Reports: "Reports come from stored history, which is not configured in this build." <br>• Automation: "Saved rules do not reach any hardware on this deployment." <br>• Analytics' By space card: no stored history. <br>• Control and the shed panel: each relay is marked `NOT DISPATCHED` or "not commandable". | Workstation (mock) | `ibems-mock-faults` plus a dev server with the database variables empty; page text read in the in-app browser | 2026-09-24 | Confirmed |
| E-175 | **Accessibility guards:** <br>• `test/contrast.test.mjs` checks every text token against every surface, to WCAG AA, in both themes. <br>• `test/css-touch-targets.test.mjs` requires the 44 px floor under `pointer: coarse`. <br>• `src/index.css` honours `prefers-reduced-motion` and `prefers-contrast: high`. The dark theme is a manual toggle. <br>• The kiosk display measured 800 × 480 (RM-045), and the nav wraps below 860 px. <br>• The build targets ES2022. The only browser named as verified is the kiosk's Chromium 151. | Repo | the two tests; `src/index.css`; `src/components/common/StaleDataBadge.tsx` (RM-045); `vite.config.ts` (`target`); `server/ibems-kiosk.service` header | 2026-09-24 | Confirmed |
| E-136 | **The journal was volatile before RM-125 (2026-09-22).** Raspberry Pi OS ships `40-rpi-volatile-storage.conf` to spare the SD card, so a reboot erased the evidence. On 2026-09-21 two reboots left the database as the only witness to a device fault. | Edge | `/etc/systemd/journald.conf.d/50-ibems-persistent.conf` header (E-035) | 2026-09-24 | Confirmed |

## Identifiers and legacy documents (A5)

| ID | Claim | Source | Command or file | Date | Status |
|---|---|---|---|---|---|
| E-100 | No tracked file and no commit message contains the database project reference, the mesh network name, a JWT, a database host name or a private-key block. | Repo | identifier scan over `git ls-files` and `git log`, matching without printing [ident-scan] | 2026-09-23 | Confirmed |
| E-101 | **Real private device addresses** from the site's LAN map appear in two committed test fixtures, `server/fleetRecover.test.mjs` and `server/lanMap.test.mjs` (committed 2026-09-22). A real deploy-target LAN address appears in a comment in `node-red-bridge/deploy.mjs`. All are RFC 1918 addresses. | Repo + Edge | exact-match check of `server/data/lan-map.json` addresses against tracked files (values not printed) | 2026-09-23 | Confirmed |
| E-102 | The parent-folder documents, which are untracked and outside git, pass the identifier scan, except `ibems-reports-prompt-review.md`, which holds one mesh address. | Workstation | [ident-scan §parent-folder] | 2026-09-23 | Confirmed |
| E-103 | The legacy HTML anatomies say "six routes" and "ten stores" (see E-060 for the current counts). The System Dossier says Mosquitto has been "bound to loopback since 26 Aug", and that Home Assistant, "MQTT as the device bus" and TimescaleDB "were all dropped". | Workstation | text extraction [doc-profile]; context grep | 2026-09-23 | Confirmed |
| E-104 | `ROADMAP.md` EX-131, `CLAUDE.md` §Site facts, `SECURITY.md` §How the deployment is arranged and `docs/pi-session-brief.md` (the Broker row and the "do not widen" rule) all state that the broker is loopback-only. E-028 contradicts this. | Repo | `grep -niE 'mqtt\|mosquitto\|broker' SECURITY.md CLAUDE.md docs/pi-session-brief.md`; `ROADMAP.md` EX-131 | 2026-09-23 | Confirmed |
| E-105 | `ibems-tracker.html` and `iBEMS-Handbook-02-Control-Strategy.html` contain `localStorage` code. The tracker prefers stored state over its own file. | Workstation | [doc-profile]; memory note on the tracker | 2026-09-23 | Confirmed |
| E-106 | `docs/pi-session-brief.md` still leads with "State as of 2026-09-03 — the fleet is down". | Repo | `docs/pi-session-brief.md` outline | 2026-09-23 | Confirmed |
