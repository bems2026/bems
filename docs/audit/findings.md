---
title: Audit findings
purpose: Problems found by the Phase A audit, by severity, each with evidence and a concrete fix
audience: [administrator, integrator]
status: Draft
last_verified: 2026-09-24
applies_to: repo 2f4c570 · edge checkout fcb1ff6
evidence: [E-002, E-012, E-013, E-015, E-016, E-018, E-019, E-021, E-022, E-023, E-025, E-026, E-027, E-028, E-029, E-030, E-031, E-033, E-034, E-035, E-040, E-041, E-042, E-043, E-044, E-045, E-046, E-050, E-053, E-054, E-058, E-065, E-070, E-073, E-077, E-078, E-079, E-080, E-081, E-083, E-084, E-085, E-089, E-090, E-101, E-103, E-104, E-105, E-106, E-121, E-123, E-139, E-153, E-154, E-160, E-161, E-163, E-164, E-169, E-170, E-171, E-172, E-176, E-178, E-179, E-180, E-184, E-194, E-197, E-198, E-200]
---

# Findings

The documentation session fixes none of these. Changing code, flows, edge services, network settings or
database contents is out of scope (prompt §1, G5). Each finding names who can fix it and how.
"Code change" means a change to the repository. Changes on the edge are the operator's to make.

**Severity** (prompt §8):
- **Critical:** safety, security or data-loss risk.
- **High:** the docs contradict the as-built system on a core path, or data could be lost.
- **Medium:** a stale reference, a missing procedure or an unverifiable claim.
- **Low:** naming, formatting or duplication.

## Critical

| ID | Layer / plane | Observation | Evidence | Cause | Concrete fix | Code change? |
|---|---|---|---|---|---|---|
| F-001 | X1 · L2 · L3 | **The MQTT broker accepts anonymous connections on all interfaces.** That includes the dedicated 2.4 GHz device segment, so anything associated with that Wi-Fi can read every topic and publish to any of them. This reverses EX-131 (2026-08-26). On 2026-09-17 the loopback listeners were commented out and `conf.d/bems.conf` gained `listener 1883 0.0.0.0` with `allow_anonymous true`. No enabled flow node uses the broker, and the preflight does not check it. | E-028, E-025, E-030, E-073, E-029 | The config is **Confirmed**. Who changed it and why is **Hypothesis**: the change falls on the day of the IR-hub and inverter-palette work. The journal was volatile then, so there is no record. | **Operator, on the edge:** back up both files. Then restore `listener 1883 127.0.0.1` and `listener 1883 ::1` in `mosquitto.conf`, and remove the `0.0.0.0` listener from `conf.d/bems.conf`. If something on the LAN genuinely must publish, add instead a listener on the LAN address with `password_file` and `allow_anonymous false`. Nothing enabled uses the broker today, so stopping and disabling it until the inverter bridge needs it is also sound. Restart `mosquitto`, then verify that `ss -tln \| grep 1883` shows only loopback. **Repository:** add a `broker_not_exposed` check to `npm run preflight`, mirroring `bridge_not_exposed`. | Yes (preflight check). Edge config: operator |
| F-026 | X1 · L4 · L5 | **Anyone could create an account, and any account can switch the building's loads.** <br>• Public sign-up was open until 2026-09-24 13:03: an outsider needed only an email address they control. <br>• The database address and public key are in the page every viewer loads. <br>• No role or allowlist is narrower than "signed in" (E-163). <br>• A new account can therefore write and arm a schedule, and the edge's scheduler fires it on real hardware, because dispatch is enabled. It can also send commands directly wherever it can reach the proxy. Writing schedules needs only internet access, not a place on the site's network. | E-169, E-176, E-170, E-171, E-163, E-164, E-041 | **Confirmed** from the auth settings, the policies and the code. Whether any account already exists that nobody recognises was **not checked**: the account read was blocked in this session (Q-17). | **Done, 2026-09-24 13:03:** the operator turned off new-user sign-ups (*Authentication → Sign In / Providers → Allow new users to sign up*), read back as `disable_signup: true` (E-176). Existing accounts keep working, and an administrator now adds each new account by invitation. **Still open, so still Critical:** review the account list and delete any account you do not recognise (Q-17). The database refuses to delete an account that has acted, so ban one of those instead (E-184). Then check `schedules.updated_by` and `commands.requested_by` for ids outside that list. **Repository:** make `npm run preflight` read `/auth/v1/settings` and fail while `disable_signup` is false. Later, add an allowlist or a role below "signed in" (X1). | Yes (preflight check; roles). The auth setting: operator |

*F-002 was Critical at GATE A and is now Medium: see the Medium table.*

## High

| ID | Layer / plane | Observation | Evidence | Cause | Concrete fix | Code change? |
|---|---|---|---|---|---|---|
| F-003 | L3 | **The edge has thermally throttled since boot.** It read 80.3 °C, and `get_throttled` shows soft-limit, throttling and frequency-capping events. The kiosk's Chromium is the largest steady CPU load: renderer plus GPU process average about 75 % of one core since boot. Throttling slows the polls and ingest, and heat shortens the card's and the board's life. | E-015, E-016 | The temperature, the flags and the kiosk's CPU are **Confirmed**. That the kiosk is what drives the temperature is **Hypothesis**. | **Site visit:** check the enclosure, heatsink and airflow, and fit a fan case if there is none. **Measure:** sample `vcgencmd measure_temp` and `top` over a working day. If the kiosk's animated or 3D cards dominate, give the kiosk a lighter profile. **Repository:** add temperature and throttle flags to `preflight`. | Optional |
| F-004 | L4 | **The database plan tier and current size are unknown.** On the Free plan, above 500 MB the project turns **read-only**: ingest writes fail and recording stops. The Free plan also has no automatic backups. `readings` adds about 28,600 rows a day, bounded at 30 days, but the hourly and report tables grow for ever. | E-089, E-090, E-080, E-081, E-070 | **Hypothesis.** The risk depends on two unmeasured facts. | **Operator:** run the size query in `open-questions.md` Q-01 and record the tier. Then complete the sizing method in the prompt §9 (L4). If the project is on Free, add a size figure to `ingestion_health` or `preflight`, and weigh Pro for its daily backups. | No to measure. Yes to monitor |
| F-005 | Docs · X1 | **Four current documents say the broker is loopback-only.** They are `ROADMAP.md` EX-131, `CLAUDE.md` §Site facts, `SECURITY.md` and `docs/pi-session-brief.md`, whose "Broker" row is what a Pi session trusts. The System Dossier outside the repo says the same. | E-104, E-103, E-028 | **Confirmed.** The documents were true on 2026-08-26, and the host changed on 2026-09-17 with no diff and no alarm. | Fix F-001 first, then re-verify the documents. If the operator decides to keep a LAN listener, update all four in one change. Either way, the check added under F-001 is what keeps them true afterwards. | Docs only (plus F-001's check) |
| F-028 | L5 · X2 | **The app says a shed group does nothing, but auto-shed acts on it.** The device panel's Metadata tab shows "Recorded only — nothing sheds automatically from this yet" under *Load-shed group*. The same column is what the Automation page's tiers panel writes and what the scheduler sheds by (953 `dsm_autoshed` commands so far, E-084). A person who trusts the hint may give a critical load a group, believing it harmless. Auto-shed will switch it off unattended. | E-178, E-084 | The contradiction is **Confirmed** in code. Why it stands is **Hypothesis**: the hint was probably written before auto-shed read this column. | **Code change:** replace the hint with what is true. For example: "Auto-shed switches this off, in group order, when a demand limit is armed on Automation." Or remove the field from Metadata and link to the tiers panel, so that one editor remains. | Yes |
| F-034 | X3 · L4 | **Nothing outside the edge notices when the edge stops recording.** Every out-of-dashboard notice is sent by the edge itself (E-200). If the edge loses power, its card fails, or ingest stops, recording stops, and no notice is sent. The gap surfaces only when someone opens the dashboard or a report's coverage. The kiosk, which runs on the edge, goes dark with it. | E-200, E-085 | **Confirmed** in code. How long such a gap has gone unnoticed on site was not measured. | **Add one check that runs somewhere else.** For example, a scheduled database job, or a small CI or uptime job, that reads `ingestion_health.last_success_at` and pushes a notice when it is older than about 10 minutes. Until then, X3's daily check is a person looking at the header pill and the last-seen times. | Yes |

## Medium

| ID | Layer / plane | Observation | Evidence | Cause | Concrete fix | Code change? |
|---|---|---|---|---|---|---|
| F-002 | X3 · X1 | **The site's credentials are backed up off the card, but no restore from that copy has been tested, and the practice is written nowhere.** The credentials are `flows_cred.json` and the `credentialSecret` that decrypts it, the device local keys (in `flows.json` and `server/data/device-credentials.json`) and `server/.env`. On the edge they sit, with every flow backup, on one 2018-dated SD card. `backup-policy.md` covers only the database. Downgraded from Critical on 2026-09-24, when the operator stated that a complete copy exists off the card (E-079). | E-044, E-045, E-043, E-019, E-012, E-079 | The on-card state is **Confirmed**. That the copy is complete and restorable is **Hypothesis** until a restore is performed (R4). | Write the existing practice into `backup-policy.md` (what is copied, where, how often, who holds it; locations only, no values), and make sure the copy is encrypted. Run one restore drill onto a spare card and record the date. `X3-operations.md` links to both. | No (procedure) |
| F-006 | X2 · L4 | **19 auto-shed commands are stuck at `dispatching`.** All are `dsm_autoshed`, and the latest is 2026-09-23 04:52 UTC. The audit trail cannot say whether those relays moved. | E-084, E-065 | The rows are **Confirmed**. That the final status update or the dispatch reply did not complete is **Hypothesis**. | Read `journalctl -u ibems-scheduler` at those timestamps (the journal has been persistent since 2026-09-22) and match them against the device's reported state. If there is a pattern, fix the status-update path in `server/auditedDispatch.mjs` or `scheduler.mjs`. | Maybe |
| F-007 | L2 · X1 | **VNC listens on all interfaces, including the device segment.** It authenticates with the service account's password through PAM. | E-033, E-025 | **Confirmed.** This is the default `wayvnc` configuration. | Bind `wayvnc` to the mesh interface or to loopback and reach it through the mesh network, or disable it if nobody uses it. | No |
| F-008 | X1 | **Whether SSH accepts passwords is unverified.** The cloud-init drop-in is root-only. | E-034 | **Unverified.** | **Operator:** run `sudo sshd -T \| grep -i passwordauthentication` and record the answer. If it is `yes`, disable it: keys and Tailscale SSH are already in place. | No |
| F-009 | X1 | **Real private device addresses are committed.** Two test fixtures (`server/fleetRecover.test.mjs`, `server/lanMap.test.mjs`) and a comment in `node-red-bridge/deploy.mjs` hold addresses from the site's own LAN. `CLAUDE.md` forbids committing IP addresses. They are RFC 1918 addresses, so they cannot be reached from the internet, but they tie the public repo to the site's address plan. | E-101 | **Confirmed.** | Replace them with RFC 5737 documentation addresses (`192.0.2.0/24`) and run `npm run test:server`. Phase D's identifier scan should cover `test/` and `server/*.test.mjs` as well as `docs/`. | Yes |
| F-010 | L3 · X3 | **Some host configuration exists only on the edge**, declared nowhere in the repository: the `tailscale-watchdog` service, timer and script (every 2 min); the broker configuration; the VNC configuration; lightdm autologin, which the kiosk user unit depends on; and the persistent-journal drop-in, which `preflight` checks for but nothing installs (E-139). A rebuild loses them with no diff. This is the exposure shape `CLAUDE.md` already names for `findTimeout` and `uiHost`. The kiosk unit itself **is** declared: it matches the repo byte for byte. | E-022, E-023, E-028, E-033 | **Confirmed.** | Capture the watchdog as a template in the repository, alongside `server/*.service`. Document the broker, VNC and autologin settings in the edge chapter. Add a preflight check for each that has a safety effect: the broker listener, and whether VNC is bound off the device segment. | Yes |
| F-011 | L3 | **The live flow has no `catch` nodes.** An exception in a function node is logged but not routed anywhere. Detection relies on status nodes and the bridge's freshness signals. | E-054 | **Confirmed.** | Decide, then document the decision. Either the generator adds a per-tab `catch` → health path (it already writes collectors onto the source tabs), or the edge chapter states that staleness and freshness are the detection mechanism, with evidence that they catch a failing parser. | Maybe |
| F-012 | L3 | **The committed baseline of the hand-built tabs is stale**: 271 nodes against 303 live, with 283 field differences. It is the repository's only copy of those four tabs, and `test/dp-parser-plan.test.mjs` reads it. | E-053 | **Confirmed.** | Run `npm run capture-flow:pi -- --host=127.0.0.1` on the edge. It is read-only against Node-RED and rewrites the redacted baseline. Then run `npm run test:bridge` and commit the refreshed baseline. | Repository data refresh |
| F-013 | L4 | **Offline readings keep a held value.** 36,041 rows have `online=false` with `power_w = 0`, 5,389 of them in the last 7 days. The honesty rule ("a gap is a gap, not a zero") holds only because every aggregate filters on `online`. A new consumer that forgets the filter will count zeros: an export, the query cookbook, a spreadsheet. | E-083 | **Confirmed.** | State the rule in `04-data.md` and use it in every cookbook query. Optionally add a view that nulls measured fields when `online` is false. | Optional |
| F-014 | L3 · X1 | **Unused services are running.** Docker and containerd (with 0 containers), rpcbind on all interfaces, CUPS, cups-browsed and ModemManager. A `solarman-device` config node also polls an inverter logger that is not on the network, and logs a timeout every 10 min. | E-031, E-025, E-040, E-018 | **Confirmed.** | The operator decides. Disable what the inverter work (RM-026) does not need, and disable the solarman node until the logger is on the device network. | No |
| F-015 | L3 · X3 | **The storage is a consumer SD card of unknown endurance**, dated 06/2018. It holds the root filesystem, a 200 MB persistent journal, 22 MB of Node-RED context (the bridge's 3.7 MB context file is rewritten continuously) and every flow backup. The last boot ran an unclean-shutdown orphan cleanup. | E-012, E-013, E-035, E-046, E-044 | **Hypothesis** (wear risk). | Image the current card as a spare now. Plan an endurance-rated card or USB-SSD boot, and put a UPS in front of the edge for clean shutdown. This procedure belongs in `03-edge.md`. | No |
| F-016 | Docs | **`docs/pi-session-brief.md` opens with stale state.** Its first section is "State as of 2026-09-03 — the fleet is down", and it is the file a Pi session reads first. | E-106 | **Confirmed.** | Move the dated states under a "History" heading, and keep the current state short and dated. Run `npm run test:bridge` afterwards, because a test reads this file. | Docs only. **Addressed 2026-09-24 (S3):** the state was moved to History and the Broker row flagged. |
| F-024 | X1 · L2 | **Real Wi-Fi network names were committed.** `docs/outage-recovery.md` named both of the edge's networks, the device SSID and the office 5 GHz SSID, in backticks, and `ROADMAP.md` named the device SSID twice. G2 forbids SSIDs in committed files. Both names coincide with public names, so exploitability is low. | E-153 | **Confirmed.** | **Fixed 2026-09-24 (S3):** each replaced with "the device SSID" or "the office 5 GHz SSID". The pre-commit scan now matches the edge's Wi-Fi profile names without printing them. | Docs only |
| F-025 | X1 · L2 | **Every device signed into the mesh account can open a shell on the edge, and the service account has passwordless sudo.** On 2026-09-24 Tailscale SSH accepted a session as a local user other than the service account. The tailnet still lists client devices unseen for 36–37 days. A lost laptop or phone signed into that account is therefore full control of the building controller, gated only by the browser re-check (E-154). | E-160, E-154, E-027 | The access is **Confirmed**. What the policy file allows beyond this is **Unverified**. | **Operator:** in the tailnet admin console, restrict SSH to named admin devices (or a tag) and to the service user only, and keep check mode on. Remove the stale nodes. Consider limiting the service account's sudo to the service restarts it needs. Record the policy in X1-security.md, by rule, not by address. | No |
| F-027 | X1 · L5 | **Break-glass sign-in is not rate-limited, and nothing records whether an attempt succeeded.** The proxy logs each `POST /api/local-login` with its origin only. It answers on every interface and through the mesh Serve path, while the login page calls it "LAN only". The session it issues can read but not command, which bounds the damage. | E-172, E-025, E-026, E-042 | **Confirmed** in code. The name `BREAK_GLASS_PASSWORD_HASH` is declared on the edge (E-042). Whether it holds a value, and whether it has been used, was not read (Q-18). | **Repository:** log the outcome of each attempt, and back off after repeated failures. **Operator:** answer Q-18. If break-glass is kept, store its password with the other credentials (F-002) and rotate it when someone who knows it leaves (X1). | Yes |
| F-029 | L5 · X1 | **`.env.example` hides that the build's database variables are also the sign-in switch, and it cites a dropped policy.** A build made without `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY` serves the dashboard with **no sign-in**, and it tries to reach Node-RED directly. The example file presents leaving them unset as a choice about history ranges only. It also says the public key is safe because of `phase4_anon_read.sql`, whose policies phase5 dropped. The build reads the checkout's root `.env` files, which `server/.env` does not feed. | E-180, E-161 | **Confirmed.** The comment dates from Phase 4, before the sign-in gate. | **Code change (comment only):** say that the two variables turn sign-in on, and that a production build must have them. Point the safety note at row-level security as it stands (phase5 onward, E-161). **Repository:** have `npm run preflight` or the build fail when a production build has no database variables. | Yes |
| F-031 | X2 | **The written priority order is only partly how the code behaves.** `X2a-control-strategy.md` §4 puts a manual override above demand shedding and a safety interlock (minimum off-times) above everything. As built there is no arbiter (E-194). Auto-shed re-sheds a load a person has just switched back on, every 15 s while the breach lasts, and no minimum off-time exists. A schedule's "on" edge also switches on a relay that auto-shed had shed. The aircon loop is the one part that yields to people and schedules. | E-194, E-123 | **Confirmed** in code. Whether any of this has happened on site was not checked. | **Decide, then record it:** either implement the order (auto-shed skips a relay with a recent `ibems-app` command, as the aircon loop already does; add a minimum off-time for loads that need one), or amend the strategy chapter to the as-built order. `X2-control-logic.md` states the as-built behaviour meanwhile. | Yes, if the order is implemented |
| F-033 | X2 · L4 | **Tightening the room-temperature floor on Settings does not stop an existing aircon rule below it.** Rules are checked against the live floor when they are saved. The loop's second check, which exists to catch a rule that "predates a policy tightening", reads the floor compiled into the build (E-198). Raise the floor on Settings, and a rule saved below the new floor keeps running until someone re-saves it. | E-198, E-121 | **Confirmed** in code. No such change has been made on site. | **Code change:** give the scheduler the proxy's live policy (`createLivePolicy`) and pass `livePolicy.current()` to `planSetpoint`. Until then, re-save every aircon rule after changing the floor. | Yes |

## Low

| ID | Layer / plane | Observation | Evidence | Cause | Concrete fix | Code change? |
|---|---|---|---|---|---|---|
| F-017 | L3 | `server/ibems-ingest.service`, both in the repository and as installed on the edge, still opens with "NOT installed automatically (no live Supabase project exists yet …)". | E-021 | **Confirmed.** | Rewrite the header comment. Reinstall the unit at the next restart window. | Yes (comment) |
| F-018 | L4 | The Sheets mirror's quota and per-file cell headroom are unmeasured. It makes 11 appends every 180 s. | E-058 | **Unverified.** | Record each sheet's row count against the provider's limits. Confirm that ADR-001's Sheets position still describes the mirror's purpose. | No |
| F-019 | L2 · L3 | The mesh path to the edge is relayed rather than direct, and the edge reports that it cannot reach its configured DNS servers. | E-002, E-027 | The observation is **Confirmed**. The cause is **Hypothesis**: egress filtering or resolver configuration. | Check the edge's resolver configuration. Ask campus IT whether outbound UDP for the mesh is filtered. | No |
| F-020 | X3 | The mesh agent on the edge updates itself unattended, and the node key never expires. Both are defensible for a server, but neither is written down. | E-027 | **Confirmed.** | State both in the update policy (`X3-operations.md`) and the access chapter (`X1-security.md`). | No |
| F-021 | Docs | The legacy HTML documents outside the repo carry stale counts and the loopback broker claim. The tracker reads `localStorage` in preference to its own file. | E-103, E-105 | **Confirmed.** | Phase S: mine, re-verify and archive them outside git (decided 2026-09-23). | No |
| F-022 | X1 | The edge's desktop session doubles as a general-purpose browser. Its non-kiosk Chromium profile holds data for general web sites. On the controller of a building's relays, every site visited is attack surface. | E-077 | **Confirmed.** | State in `X1-security.md` that the edge is not a workstation. Anyone who needs a browser on site uses a separate device, and the physical desktop stays kiosk-only. | No |
| F-023 | X2 | `server/dispatchLight.mjs`'s header still says "Outlets/ACU have no equivalent endpoint yet". The same file routes outlets to `/outlet/:target` and the aircon to `/acu`, and `DISPATCH_CLASSES` includes both. | E-078; `server/dispatchLight.mjs` | **Confirmed.** | Rewrite the header comment. | Yes (comment) |
| F-030 | L5 | The device panel's Metadata tab says "No spaces defined yet — add them from Spaces **on this page**". Spaces moved to Settings, reached from the account menu. `docs/floor-plan-design.md` still places the page-card toggles on Devices. | E-179 | **Confirmed.** | **Code change:** name Settings → Spaces in the hint. The design note gets a dated as-built line (done in this change). | Yes (the hint) |
| F-032 | X2 · L5 | The aircon loop's `acu_offline` explanation, which the Automation page shows, still says the IR blaster "has never been paired to the vendor account". `server/acuLoopPlan.mjs`'s header says the same. The hub was paired on 2026-09-17 and drives the unit today. | E-197 | **Confirmed.** | **Code change:** reword it to what is true whenever the unit reports offline, for example "The aircon's IR hub is not answering, so no step can be sent". | Yes |
