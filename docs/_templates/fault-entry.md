<!-- One row of a chapter's "How it fails" fault matrix (R2). All five columns are required. A fix column that says
     "check the connection" is not a fix (Appendix A). Copy the row into the chapter's table, and add it to
     91-troubleshooting-index.md under its symptom. -->

| Symptom | Likely cause | Check that tells the causes apart | Fix | How to confirm it held |
|---|---|---|---|---|
| <What the user sees, in their words — "the dashboard shows dashes for one outlet"> | <The most likely cause first; mark Confirmed or Hypothesis if it came from the pilot> | <One observation or command whose result differs between the candidate causes — and what each result means> | <The action, specific enough to perform — a command, a setting, a part> | <What to observe, and for how long, before calling it fixed — e.g. "reports again and stays reporting past the offline threshold (600 s)"> |
